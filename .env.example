# Optional: Turso database for hosted deployments (e.g. Vercel)
# Leave unset for local development — the app will use a local SQLite file.
# TURSO_DATABASE_URL=libsql://your-database.turso.io
# TURSO_AUTH_TOKEN=your-auth-token
